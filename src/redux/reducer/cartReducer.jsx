const INITIAl_STATE = {
    cart: []
}

export default function cartReducer(state = INITIAl_STATE, action) {
    switch (action.type) {
        case "ADDITEM" :
            return{

            }

        case "UPDATEITEM" :
            return {

            }
    }
}